import { createTheme } from '@mui/material/styles';

export const customTheme = createTheme({
  palette: {
    textfield:{
        main: "#ffffff",
        name: "#ffffff",
        color: "#ffffff"
    },
  },

});

